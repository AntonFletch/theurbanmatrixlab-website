const form=document.querySelector('#login-form');const message=document.querySelector('#login-message');umxDb.auth.getSession().then(({data})=>{if(data.session)location.href='dashboard.html'});form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='Signing in…';const values=new FormData(form);const {error}=await umxDb.auth.signInWithPassword({email:values.get('email'),password:values.get('password')});if(error){message.textContent=error.message;return}location.href='dashboard.html'});
